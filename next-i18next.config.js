// const path = require('path');

module.exports = {
  i18n: {
    locales: ['ko', 'en', 'ja'],
    defaultLocale: 'en'
    // localeDetection: false
  }
  //   trailingSlash: true,
  // localePath: path.resolve('./public/locales')
};
